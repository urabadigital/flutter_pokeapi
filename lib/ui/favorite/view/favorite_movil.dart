import 'package:flutter/material.dart';

class FavoriteMovil extends StatelessWidget {
  const FavoriteMovil({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('favorite movil'));
  }
}

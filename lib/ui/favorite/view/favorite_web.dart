import 'package:flutter/material.dart';

class FavoriteWeb extends StatelessWidget {
  const FavoriteWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('favorite web');
  }
}

import 'package:flutter/material.dart';

class SettingMovil extends StatelessWidget {
  const SettingMovil({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('setting movil'));
  }
}

import 'package:flutter/material.dart';

class SettingWeb extends StatelessWidget {
  const SettingWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('setting web');
  }
}
